# 🌐 Infraestructura Finconecta (Terraform + AWS)

Despliega tu backend Spring Boot con PostgreSQL (RDS) en AWS.

---

## 🚀 Despliegue rápido

```bash
cd infra/terraform
terraform init
terraform plan -out=tfplan
terraform apply tfplan
```
